import React from 'react';

import { Icon } from 'semantic-ui-react';

const Menu = () => {
    return (
        <nav>
            <Icon size="large" name="bars" />
        </nav>
    );
}

export default Menu;